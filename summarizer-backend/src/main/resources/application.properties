spring.application.name=summarizer-backend

# Load token from secrets.properties (local dev) — file is gitignored
# In Docker/CI, set env var HUGGINGFACE_TOKEN instead (Spring maps it automatically)
spring.config.import=optional:classpath:secrets.properties
